
// <%= name %> Wiretap
// -------------------------------------------------- //

App.Wiretap("<%= (name === "root") ? "" : name.toLowerCase() %>", function() {

<% data.forEach(function(d) { %>
    this.<%= d %> = function(req, res) {

    };
<% }) %>

});
