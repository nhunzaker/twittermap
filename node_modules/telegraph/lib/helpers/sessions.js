// The sessions helper
// -------------------------------------------------- //

module.exports.hash = function(context) {
  
  var bcrypt = require("bcrypt")
  ,   salt = bcrypt.gen_salt_sync(10)
  ,   hash = bcrypt.encrypt_sync(Date.now().toString(), salt);
  
  return hash;

};
