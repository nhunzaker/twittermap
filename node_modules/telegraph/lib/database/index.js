// Telegraph Database
// -------------------------------------------------- //

module.exports.setup = function(server) {

    var connection = {}
    ,   adapter = server.database.adapter
    ;

    switch(adapter){

    case "mongo": case "couch": case "redis":
        connection = require("./adapters/" + adapter).install(server); 
        break;
        
    default: 
        connection = require("./adapters/static").install(server);
    }

    return connection;

};