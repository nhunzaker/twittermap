// The <%= name %> Design Document
// -------------------------------------------------- //

Media = App.Model("<%= name.toLowerCase() %>", {
  
  validate_doc_update: function (newDoc, oldDoc, usrCtx) {

  },
  
  views: {

    all: {
      map: function(doc) { 
        if (doc.type === 'media') { 
          emit(null, doc);
        }
      }
    }

  }

});
